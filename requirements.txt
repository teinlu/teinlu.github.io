# Core numerical / ML
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0
torch>=2.0.0
matplotlib>=3.7.0
tifffile>=2023.3.0

# Optional: FAISS for fast nearest-neighbor memory-bank scoring
# (code falls back to sklearn KDTree if unavailable)
# faiss-gpu>=1.7.0


